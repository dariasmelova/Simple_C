// a program that prints the numbers from 1 to 10 side by side on the same line with three spaces between numbers

#include <stdio.h>;

int main(void)
{
	int counter = 1;

	while (counter <= 10) {
		printf("%d   ", counter++);
	}
}
