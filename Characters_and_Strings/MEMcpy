// a program demonstrating function memcpy

#include <stdio.h>
#include <string.h>

int main(void)
{
    char s1[15]; 
    char s2[] = "String to copy"; 

    memcpy(s1, s2, 15);
    printf("%s\n%s\"%s\"\n",
        "After s2 is copied into s1 with memcpy,",
        "s1 contains ", s1);
}
